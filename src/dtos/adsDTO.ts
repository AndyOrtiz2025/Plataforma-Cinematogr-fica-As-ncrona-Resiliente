/* =====================================================================
   dtos/adsDTO.ts
   Forma cruda tal como la entrega el servicio simulado de Anuncios.
   ===================================================================== */

export interface AdsServiceDTO {
  banner: string;
}
